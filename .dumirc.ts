import { defineConfig } from 'dumi';

export default defineConfig({
  base: '/react-utils-js/',
  publicPath: '/react-utils-js/',
  outputPath: 'docs-dist',
  themeConfig: {
    name: 'react-utils-js',
  },
});
